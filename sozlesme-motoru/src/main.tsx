import React from 'react';
import ReactDOM from 'react-dom/client';
import { ContractFormModule } from './ContractFormModule';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <ContractFormModule />
  </React.StrictMode>,
);
